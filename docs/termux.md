# KamaClaude Termux ????

??????? Android Termux ?????? KamaClaude?????? `uv sync` ????? Python ????????????? dev ?????

## ????

??????????

```sh
sh scripts/setup_termux.sh
```

??????

```text
python rust clang make pkg-config openssl libffi git uv
```

????? Termux ?? Python?

```sh
uv python pin "$(command -v python)"
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

????

- `--python "$(command -v python)"`??? uv ???? Linux Python?
- `UV_PYTHON_DOWNLOADS=never`??? uv ???? Python??? Android ??????
- `UV_LINK_MODE=copy`??? Termux/Android ???? hardlink ??????
- `--no-dev`???????????????? pytest?ruff?mypy ??????

??????? Termux ????????????

```sh
uv sync --dev --python "$(command -v python)"
```

## ????

```sh
pkg update -y
pkg upgrade -y
pkg install -y python rust clang make pkg-config openssl libffi git
python -m pip install -U pip setuptools wheel uv
uv python pin "$(command -v python)"
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

???????

```sh
uv sync --python 3.13
```

???? uv ????? CPython?Termux ???????? `pkg install python` ????? Python?

## ?? API

?????????

```sh
cp .env.example .env
```

OpenAI ????????

```env
KAMA_LLM_PROVIDER=openai
KAMA_LLM_DEFAULT_MODEL=gpt-4o-mini
OPENAI_API_KEY=sk-...
# OPENAI_BASE_URL=https://your-openai-compatible-endpoint/v1
```

Anthropic ???

```env
KAMA_LLM_PROVIDER=anthropic
KAMA_LLM_DEFAULT_MODEL=claude-sonnet-4-6
ANTHROPIC_API_KEY=sk-ant-...
# ANTHROPIC_BASE_URL=https://your-anthropic-compatible-endpoint
```

## ??

??? Termux ???? core?

```sh
uv run kama-core
```

??? Termux ???????

```sh
uv run kama ping
```

?????

```sh
uv run kama chat
uv run kama run --goal "??????????"
uv run kama-tui
uv run kama-web
```

Web UI ?????

```text
http://127.0.0.1:7440
```

????????????????? Termux ????????Web UI ????? Python ????????? `kama-core`????? JSON-RPC ? SSE ?????

## ????

### uv ?????? Python

???

```sh
uv python pin "$(command -v python)"
UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

?????????????????????

```sh
rm -rf .venv
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

### Rust ? maturin ????

?? Termux ? Rust ??????

```sh
pkg install -y rust clang make pkg-config
rustc --version
cargo --version
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

### OpenSSL ? libffi ????

??????

```sh
pkg install -y openssl libffi pkg-config
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
```

### hardlink warning

?????

```text
Failed to hardlink files; falling back to full copy
```

??????????????????

```sh
export UV_LINK_MODE=copy
```

### TUI ?????

?????????TUI ????????????????

```sh
uv run kama chat
```

??? Web UI?

```sh
uv run kama-web
```

## ??????

```sh
pkg update -y
pkg install -y python rust clang make pkg-config openssl libffi git
python -m pip install -U pip setuptools wheel uv
cd ~/KamaClaudePro
UV_LINK_MODE=copy UV_PYTHON_DOWNLOADS=never uv sync --no-dev --python "$(command -v python)"
cp .env.example .env
uv run kama-core
```

???? Termux ???

```sh
cd ~/KamaClaudePro
uv run kama ping
uv run kama chat
```
